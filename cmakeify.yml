targets: [iOS, android]
buildTarget sqlite
android:
  ndk:
    runtimes: [c++, gnustl, stlport]
    platforms: [12, 21]
example: |
   #include <sqlite3.h>
   void test() {
     sqlite3_initialize();
   }
   

